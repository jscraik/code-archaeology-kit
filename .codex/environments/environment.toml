# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "code-archaeology-kit local environment"

[setup]
script = '''
set -euo pipefail

echo "[codex] setup: /Users/jamiecraik/dev/code-archaeology-kit"
bootstrap_node_tools() {
  export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.local/share/mise/shims:$HOME/.local/bin:$PATH"

  if ! command -v node >/dev/null 2>&1; then
    if command -v mise >/dev/null 2>&1; then
      eval "$(mise activate bash)" >/dev/null 2>&1 || true
    fi
  fi
}

run_node_pm() {
  bootstrap_node_tools
  npm "$@"
}

run_node_pm ci || run_node_pm install

if command -v uv >/dev/null 2>&1; then
  uv sync || true
elif [ -f requirements.txt ]; then
  python3 -m pip install -r requirements.txt || true
fi
'''

[[actions]]
name = "Install deps"
icon = "tool"
command = '''
set -euo pipefail

bootstrap_node_tools() {
  export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.local/share/mise/shims:$HOME/.local/bin:$PATH"

  if ! command -v node >/dev/null 2>&1; then
    if command -v mise >/dev/null 2>&1; then
      eval "$(mise activate bash)" >/dev/null 2>&1 || true
    fi
  fi
}

run_node_pm() {
  bootstrap_node_tools
  npm "$@"
}

run_node_pm ci || run_node_pm install
'''

[[actions]]
name = "Run tests"
icon = "test"
command = '''
set -euo pipefail

if command -v uv >/dev/null 2>&1; then
  uv run pytest
else
  python3 -m pytest
fi
'''
